TODO List

* Split out libfbpanel and put shared functions into a library used by both
  fbpanel itself and its plugins
* Add CI pipeline (GitHub Actions) for multi-distro build verification

Help wanted:
* Update/add packaging artifacts (RPM spec, ebuild, Debian package)
* Integration testing on live X11 session
